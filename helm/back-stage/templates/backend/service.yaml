apiVersion: v1
kind: Service
metadata:
  name: {{ include "back-stage.fullname" . }}-backend
  labels:
    {{- include "back-stage.labels" . | nindent 4 }}
    app.kubernetes.io/component: backend
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.backend.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "back-stage.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: backend
