apiVersion: 1

providers:
  - name: back-stage
    orgId: 1
    folder: "Back-Stage"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
